// Keep typography offline-safe in local/dev environments.
// We rely on CSS font stacks instead of next/font so the app builds without
// external access to Google Fonts.
export const fontVariables = '';
